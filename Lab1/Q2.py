#2. Write a program that reads the length of the base and the height of a right_angled triangle and prints the area. Every number is given on a separate print(line)
length = int(input('Enter the length'))
height = int(input('Enter the height'))
area = 1/2 * length * height
print(f'The area ={area}')